name: MikTeX
description: MixTeX in GitHub Actions
inputs:
  documents:
    description: The directory to find TeX documents in
    required: true
runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.documents }}